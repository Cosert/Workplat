#include<bits/stdc++.h>
using namespace std;

#define cc 100000

int main() {
    int number,l,x,y;scanf("%d",&number);
    char a[cc];
    while(number--) {
        scanf("%s",&a);
        l=strlen(a);

    }

    return 0;
}
