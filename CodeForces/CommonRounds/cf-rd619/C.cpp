#pragma GCC optimize("O2")

#include<bits/stdc++.h>
using namespace std;

#define ll long long
#define pii pair<int,int>
#define lc(n) n<<1
#define rc(n) (n<<1)|1
#define inf 0x3f3f3f3f

int n,m,k,ans;

int main() {
    int T;scanf("%d",&T);
    while(T--) {
        scanf("%d%d",&n,&m);
        k=n-m;

        if(!m) printf("0\n");
        else {
            if(n&1) {
                ans
            }
            else {
                ans=(n/2)*(n/2+1)
            }
        }
    }
    return 0;
}

