#include<bits/stdc++.h>
using namespace std;

#define ch 150000

int main() {
    int number;scanf("%d",&number);
    int n;char a[ch];

    while(number--) {
        scanf("%s",&a);
        for(int i=0;i<=strlen(a)-1;i++) {
            if(a[i]=='o') {

            }
        }
    }

    return 0;
}
